# START HERE

欢迎使用这个模板。首次使用时按以下顺序操作。

1. 读 `ARCHITECTURE.md`，理解数据分层和目录责任。
2. 在 `input/profile/` 填写经历、技能、项目、资格和求职约束。
3. 将聊天中确认的新事实立即回写 `input/profile/`，不要只留在会话里。
4. 复制 `input/companies/Sample_Application/` 为新申请目录，保存 JD 快照并填写适配分析。
5. 生成 `documents/output/{后缀}/md/` 中的 Markdown，然后构建 PDF。
6. 建立 `interview/companies/{后缀}/`，一轮一文件记录。
7. 投递前运行 `check.py` 和 `audit_privacy.py`，并人工目视 PDF。

用 `Sample_Application` 理解格式后，可删除整个示例申请目录。不要把示例内容当成自己的事实。
